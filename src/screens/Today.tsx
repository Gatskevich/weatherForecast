import React, { useEffect } from 'react';
import { Text, View } from 'react-native';

export const Today = () => {
	useEffect(() => {}, []);

	return (
		<View>
			<Text>Weather forecast</Text>
		</View>
	);
};
